# Hindsight Service and Dashboard Images
# These are used for production deployment - for local development, images are built from source
HINDSIGHT_SERVICE_IMAGE=hindsight-service:latest
HINDSIGHT_DASHBOARD_IMAGE=hindsight-dashboard:latest

# ==========================================
# LOCAL DEVELOPMENT (Required for ./start_hindsight.sh)
# ==========================================

# Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=hindsight_db
DATABASE_URL=postgresql://user:password@postgres:5432/hindsight_db

# Hindsight Service Configuration
LLM_API_KEY=your_llm_api_key_here
LLM_MODEL_NAME=gemini-2.5-flash
CONSOLIDATION_BATCH_SIZE=10
FALLBACK_SIMILARITY_THRESHOLD=0.8
SUPPORT_EMAIL=support@hindsight-ai.com
SUPPORT_CONTACT_MIN_INTERVAL_SECONDS=60
PRUNING_BATCH_SIZE=20
PRUNING_MAX_ITERATIONS=10

# Email Configuration (Optional)
EMAIL_PROVIDER=resend
FROM_EMAIL=support@hindsight-ai.com
FROM_NAME=Hindsight AI
REPLY_TO_EMAIL=support@hindsight-ai.com
RESEND_API_KEY=your_resend_api_key_here
EMAIL_TEMPLATE_DIR=/app/templates
EMAIL_RATE_LIMIT_HOUR=100
EMAIL_RATE_LIMIT_DAY=1000

# Frontend Configuration
REACT_APP_HINDSIGHT_SERVICE_API_URL=http://localhost:8000

# ==========================================
# PRODUCTION DEPLOYMENT (Optional for local dev)
# ==========================================

# Cloudflare Credentials for Traefik (Production only)
CLOUDFLARE_DNS_EMAIL=
CLOUDFLARE_DNS_API_TOKEN=

# OAuth2 Proxy Credentials (Production only)
OAUTH2_PROXY_CLIENT_ID=
OAUTH2_PROXY_CLIENT_SECRET=
OAUTH2_PROXY_COOKIE_SECRET=

# ==========================================
# NOTES
# ==========================================
# For local development:
# 1. Copy this file to .env
# 2. Update LLM_API_KEY with your actual API key
# 3. Run ./start_hindsight.sh to start all services
# 4. Services will be available at:
#    - Frontend: http://localhost:3000
#    - Backend API: http://localhost:8000
#    - Database: localhost:5432
