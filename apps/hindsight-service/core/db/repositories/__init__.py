"""
Per-domain repository modules for database access.

Phase 3 scaffolding: these repositories currently delegate to existing
functions in `core.db.crud` to preserve behavior. We will gradually move the
implementations here and reduce `crud.py` to a thin facade.
"""

