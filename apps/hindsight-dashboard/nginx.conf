server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # API proxy: forward /api/* to backend service
  location /api/ {
    proxy_pass http://hindsight-service:8000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    # Forward auth headers set by oauth2-proxy so backend can identify the user
    proxy_set_header X-Auth-Request-User $http_x_auth_request_user;
    proxy_set_header X-Auth-Request-Email $http_x_auth_request_email;
    proxy_set_header X-Auth-Request-Access-Token $http_x_auth_request_access_token;
    proxy_set_header Authorization $http_authorization;

    # CSRF mitigation: for mutating methods, only allow allowed origins
    # Use non-nested ifs to satisfy nginx conf constraints
    set $block_csrf 0;
    if ($request_method ~* "^(POST|PUT|PATCH|DELETE)$") { set $block_csrf 1; }
    # If no Origin header, do not block (same-origin navigations/forms often omit Origin)
    if ($http_origin = "") { set $block_csrf 0; }
    # Allow same-origin automatically based on host
    if ($http_origin = $scheme://$host) { set $block_csrf 0; }
    if ($http_origin = "http://localhost:3000") { set $block_csrf 0; }
    # Block remaining modifying requests with disallowed Origin
    if ($block_csrf = 1) { return 403; }
    proxy_redirect off;
    proxy_read_timeout 300s;
  }

  # Guest API proxy: forward /guest-api/* to backend service without auth headers
  # Path prefix is stripped by proxy_pass just like /api/
  location /guest-api/ {
    proxy_pass http://hindsight-service:8000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    # Do NOT forward oauth2-proxy auth headers here to keep it guest/anonymous
    proxy_redirect off;
    proxy_read_timeout 300s;
  }

  # Fallback routes for API endpoints without /api prefix (frontend routing issue workaround)
  location /memory-blocks/ {
    proxy_pass http://hindsight-service:8000/memory-blocks/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Auth-Request-User $http_x_auth_request_user;
    proxy_set_header X-Auth-Request-Email $http_x_auth_request_email;
    proxy_set_header X-Auth-Request-Access-Token $http_x_auth_request_access_token;
    proxy_set_header Authorization $http_authorization;
  }
  
  location /agents/ {
    proxy_pass http://hindsight-service:8000/agents/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Auth-Request-User $http_x_auth_request_user;
    proxy_set_header X-Auth-Request-Email $http_x_auth_request_email;
    proxy_set_header X-Auth-Request-Access-Token $http_x_auth_request_access_token;
    proxy_set_header Authorization $http_authorization;
  }
  
  location /keywords/ {
    proxy_pass http://hindsight-service:8000/keywords/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Auth-Request-User $http_x_auth_request_user;
    proxy_set_header X-Auth-Request-Email $http_x_auth_request_email;
    proxy_set_header X-Auth-Request-Access-Token $http_x_auth_request_access_token;
    proxy_set_header Authorization $http_authorization;
  }
  
  location /organizations/ {
    proxy_pass http://hindsight-service:8000/organizations/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Auth-Request-User $http_x_auth_request_user;
    proxy_set_header X-Auth-Request-Email $http_x_auth_request_email;
    proxy_set_header X-Auth-Request-Access-Token $http_x_auth_request_access_token;
    proxy_set_header Authorization $http_authorization;
  }

  # Serve static assets and fall back to SPA entrypoint
  location / {
    try_files $uri /index.html;
  }

  # Runtime env.js should never be cached
  location = /env.js {
    add_header Cache-Control "no-store, must-revalidate" always;
    try_files $uri =404;
  }

  # Cache immutable built assets aggressively
  location /assets/ {
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }
}
